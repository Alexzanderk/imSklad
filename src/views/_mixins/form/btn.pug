mixin btn(text)
    #{attributes.href ? 'a' : 'button'}.btn(
        href=attributes.href 
        type=attributes.type
        class=attributes.class 
        id=attributes.id 
        form=attributes.form
        data-action=attributes.action
    )= text
